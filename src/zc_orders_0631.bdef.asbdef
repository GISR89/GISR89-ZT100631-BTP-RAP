projection;
strict ( 2 );
use draft;

define behavior for ZC_ORDERS_0631 //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use association _Items { create; with draft; }
}

define behavior for ZC_ITEMS_0631 //alias <alias_name>
{
  use update;
  use delete;

  use association _Orders { with draft; }
}